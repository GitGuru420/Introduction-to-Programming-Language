#include <stdio.h>
#include <math.h>
int main() {
    double a;
    scanf("%lf", &a);

    printf("%lf\n", ceil(a));
    printf("%lf\n", floor(a));
    printf("%lf\n", round(a));
    printf("%lf\n", sqrt(4));
    printf("%lf\n", sqrt(8));
    printf("%lf\n", pow(2,5));

    return 0;
}